---
platform: linux

image_resource:
  type: registry-image
  source: {repository: docker}

inputs:
- name: repository

run:
  path: /bin/sh
  args:
    - -cex
    - |
      apk add --update -qqq bash iptables
      repository/ci/tasks/scripts/start-docker.sh
      repository/ci/tasks/scripts/integration.sh
