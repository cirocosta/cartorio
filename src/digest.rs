use std::io::prelude::*;

/// Computes the digest of a file.
///
/// # Arguments
///
/// * `reader` - a s
///
///
fn compute(reader: impl Read) -> Result<String> {
    unimplemented!("TBD");
}

fn store() {
    unimplemented!("TBD");
}

fn retrieve() {
    unimplemented!("TBD");
}
